#ifndef __PATH_HPP__
#define __PATH_HPP__
#include <string>
#include <fstream>

namespace PATH{
    // Cannot test, works differently for different computers
    const std::string getPath(const std::string&);
}

#endif